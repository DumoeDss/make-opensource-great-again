{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: the core IPC bridge the splash uses to poll `daemon_status`. No plugin or remote permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
