# ui-session-queue Delta

## REMOVED Requirements

### Requirement: Transitional batch exit summary

**Reason**: Superseded by the `ui-batch-exits` capability — the N>1 exit step is now the real batch exit page (dual batch exit cards + batch export), and the N=1 "keeps the single-session dual exit cards" behaviour is restated there.
