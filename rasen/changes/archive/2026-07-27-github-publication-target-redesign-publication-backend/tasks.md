## 1. Publication contracts and durable stores (`packages/daemon/src/publication/**`)

- [x] 1.1 Add strict domain/public types for canonical target state, discriminated `PublicationStatus`, sealed/public preview, immutable receipt, target snapshot, manifest, journal phases, `PublicationError`, and the `GitHubPublication` interface; keep workspace, token, command, raw-output, and exact-content fields private.
- [x] 1.2 Implement the canonical github.com `owner/repo` parser and strict `.mosga-dataset.json` parser/compatibility evaluator, including bounded strings/lists, public-repository requirement, supported contract/kind, unique accepted schemas, and non-placeholder license; add table-driven invalid-input tests.
- [x] 1.3 Implement injectable file-backed and in-memory target stores with `{ schemaVersion: 1, revision, upstream|null }`, unique same-directory temp + atomic rename, monotonic/idempotent configure/clear semantics, acquisition-specific cross-process claims, and fail-closed corrupt/unreadable handling; test restart persistence, write failure, same-target no-op, clear/reconfigure and real-process revision non-reuse, stale-reclaim ABA resistance, and no path leakage.
- [x] 1.4 Implement a bounded in-memory sealed-preview store with opaque injected IDs, injected clock, default 15-minute TTL, exact private bundle/snapshot retention, expiry eviction, and target-change invalidation; test TTL boundaries, capacity eviction, restart/lost-ref behavior, and public projection excluding `files[].contents`.
- [x] 1.5 Implement versioned atomic journal and receipt stores plus an injectable publication lock, enforcing monotonic phases and immutable receipts; test corrupt/truncated files, duplicate phase/receipt writes, real-process `publish_in_flight`, dead-owner crash recovery without ABA deletion, and secret/path-free public reads.

## 2. Semantic GitHub boundary (`packages/daemon/src/publication/github/**`)

- [x] 2.1 Define `GitHubPort` domain operations for actor/repository/default-head inspection, manifest-at-commit read, verified fork inspection/creation, exact PR lookup, and PR creation, plus typed sanitized adapter failures; add a recording fake covering every status/delivery state without network writes.
- [x] 2.2 Implement the production `gh` read adapter through a backend-owned argument-array process runner: structured actor/repository/permission/default-branch/head/manifest reads with explicit repository/ref inputs, bounded JSON parsing, github.com-only normalization, and stable error mapping; test exact arguments and malicious/malformed stdout/stderr.
- [x] 2.3 Implement target route resolution: direct for writable upstream, otherwise only an actor-owned fork whose GitHub source ID equals upstream, otherwise a confirmed-submit-only fork attempt with stable policy-failure mapping; test unrelated same-name repositories, private/incompatible upstreams, missing login/client, permission changes, and actor changes.
- [x] 2.4 Implement submit-only fork provisioning and bounded propagation polling, verifying the returned fork/source identity before use and making preview/status incapable of calling the write operation; test existing-race adoption, timeout, wrong-parent refusal, and exactly-once creation through the fake.
- [x] 2.5 Implement semantic exact PR lookup through `gh api` with verified upstream repository ID/base and push repository/head identity; create with a bare direct head or `<actor>:<branch>` fork head, plus an ephemeral Git credential seam that never puts tokens in URLs, arguments, persistence, logs, or errors; test direct organization/user, fork, ambiguity, lost-response adoption, and token/path/stderr redaction.

## 3. Target status and sealed preview (`packages/daemon/src/publication/**`)

- [x] 3.1 Implement target configure/clear/inspect orchestration over the target store and GitHub resolver, returning only the specified `unconfigured`, `login_required`, `fork_confirmation_required`, `ready`, or `blocked` shapes and invalidating seals only on semantic target changes; test every state and safe issue projection.
- [x] 3.2 Implement unified review selection for 1–500 input `reviewIds`: strict non-empty/bounded validation, deterministic deduplication, per-review lookup, `applyDispositions`, gate recheck, review/session attribution, and one stamped-session collection for N=1/N>1; test empty/oversized/duplicate/missing/locked/mixed-alias inputs.
- [x] 3.3 Add a backend bundle validator for `ContributionBundle` contract v1: record/file associations and canonical order, unique raw/session/path identity, branch grammar, normalized portable `data/` paths, Windows reserved/case-fold collision checks, engine pins, UTF-8 bytes, per-file hashes, totals, and recomputed aggregate digest.
- [x] 3.4 Add adversarial bundle-validator tests for tampered contents/bytes/hash/digest/totals, duplicate or mismatched record files, traversal/backslash/control/device names, case-insensitive collisions, malformed refs, missing engine pins, and valid reviewed Unicode/percent-encoded paths that remain byte-for-byte unchanged.
- [x] 3.5 Implement `preview`: resolve the exact target repository/manifest/base commit/actor/push route read-only, verify selected schema acceptance, call `compileContributionBundle` once with the daemon ruleset/static pins and manifest license, validate/seal the full bundle and target snapshot, and project the typed HTTP-safe preview.
- [x] 3.6 Add recording-adapter preview tests proving success and every refusal perform zero filesystem/Git/GitHub/journal/receipt writes, new forks are reported but not created, N=1/N>1 share one flow, request order is deterministic, and no response contains exact contents/local paths/commands/raw output.

## 4. Managed workspace and local Git (`packages/daemon/src/publication/workspace/**`)

- [x] 4.1 Move/replace any needed process-runner behavior behind backend-owned local `ProcessRunner`/`GitWorkspacePort` seams using argument arrays and `shell: false`; remove daemon dependence on publisher delivery-runner exports and add spawn/fake-runner tests for bounded capture and sanitized failures.
- [x] 4.2 Implement locally derived managed-root layout, repository-ID-hashed bare cache names, marker-owned per-publication worktrees, fixed internal `upstream`/`push` semantics, and sealed-base-commit checkout; test slug/path independence and reject cleanup of missing-marker or out-of-root locations.
- [x] 4.3 Implement the exact-byte writer with component-by-component containment, symlink/reparse-point/junction rejection, portable destination collision defense, safe parent creation/open behavior, UTF-8 write, and post-write byte/hash verification; test traversal and link escapes using real temporary filesystems plus injectable platform/reparse probes.
- [x] 4.4 Implement explicit staging/commit from the sealed bundle using `git add -- <validated paths>`, the sealed publisher message plus full digest trailer, and captured commit/tree SHA; add local temporary-Git integration tests proving exact record/provenance bytes, no extra staged files, sealed base parent, and stable tree identity.
- [x] 4.5 Implement explicit `push` remote setup/auth and `git push push HEAD:refs/heads/<sealed-branch>` with no `origin` dependency or force push; when the branch already exists, fetch and compare the complete tree SHA before adopting or returning `branch_conflict`.
- [x] 4.6 Add local-Git/fake-auth tests for direct and fork push repositories, unrelated/missing `origin`, same-tree retry, different-tree collision, rejected push, credential non-leakage, and marker-contained success/retry cleanup; use local bare remotes only, never GitHub.

## 5. Confirmed submit, final safety gate, and recovery (`packages/daemon/src/publication/**`)

- [x] 5.1 Implement strict submit input (`publicationRef`, `targetRevision`, `contentDigest`, literal `confirmPublic: true` only) and pre-write validation order: receipt/ref lookup, seal/TTL/bindings, current target revision, current reviews/gates, same-options recompilation and exact commitments, current target snapshot, then complete bundle revalidation.
- [x] 5.2 Implement the final pre-write safety pass using `precheckRecord` on every sealed exact record body (trailing newline included) and `scanRawBytesBackstop` on every exact provenance sidecar with the sealed trusted ruleset; map all refusals to deterministic review/session/rule counts only.
- [x] 5.3 Add zero-mutation submit tests for false confirmation, extra path/remote/token fields, missing/expired refs, mismatched revision/digest, changed target/base/manifest/actor/route, missing/locked/changed reviews, engine drift, tampered bundles, and record/sidecar final-precheck refusals.
- [x] 5.4 Implement the journaled submit state machine (`validated -> committed -> fork_ready -> pushed -> pr_observed -> completed`), ensuring the first journal write occurs only after all final pre-write gates and fork creation occurs only after explicit confirmation and local commit.
- [x] 5.5 Implement direct/existing-fork/on-submit-fork delivery through workspace + semantic GitHub ports, persist the real receipt atomically, and return its target/base/branch/commit/PR/digest audit fields with no internal fields.
- [x] 5.6 Implement recovery/idempotency for existing receipts, committed workspaces, same-tree remote branches, push-success/PR-not-created, PR-created/response-lost, receipt-written/journal-not-completed, and target reconfiguration after proven push; test one PR/create maximum, exact receipt equality, conflicting-tree refusal, and no retargeting.

## 6. Daemon HTTP/CLI hard replacement (`packages/daemon/src/{app,http,publish,cli,index}.ts` and tests)

- [x] 6.1 Replace `packages/daemon/src/publish.ts` with strict route adapters for `GET /api/publish`, `PUT|DELETE /api/publish/target`, `POST /api/publish/preview`, and `POST /api/publish/submit`, including stable domain-to-HTTP mapping and strict schemas that strip no unknown authority fields.
- [x] 6.2 Wire production/injected publication dependencies in `AppOptions`/`createApp`/exports, deriving the managed root locally; delete `dataRepoPath`, `validateDataRepoPath`, publisher runner imports/options, local clone probes, stage state, origin inference, and all old per-review/batch plan-stage-submit registrations.
- [x] 6.3 Add a dispatcher-wide mutating-request guard for loopback Host + `application/json` media type + non-cross-site fetch metadata + exact optional Origin, with zero CORS; cover existing review/provider/direct-submit mutations and publication routes in `security.test.ts`.
- [x] 6.4 Replace the top-level raw-message catch and publication/process errors with curated generic/stable bodies; add canary tests whose thrown messages contain a fake token, absolute path, command, stdout, and stderr and assert none appears anywhere in HTTP JSON.
- [x] 6.5 Remove `--data-repo` parse/help/start propagation from `packages/daemon/src/cli.ts`, add unknown-option validation so both obsolete flag forms fail rather than being ignored, and update CLI tests while preserving port/no-open/provider-store behavior.
- [x] 6.6 Replace obsolete `publish.test.ts`/`publish-batch.test.ts` expectations with target/status/configure/clear/preview/submit integration tests over the real review/apply/publisher path and fake GitHub/local adapters; assert every legacy route is 404 and single/batch both use `reviewIds`.

## 7. Target template and backend-owned documentation

- [x] 7.1 Add `templates/community-data-repo/.mosga-dataset.json` with concrete supported kind/contract/schema/license values and a standalone strict manifest validation script/package check; test valid, missing, malformed, unsupported, duplicate-schema, empty, and placeholder-license fixtures without GitHub.
- [x] 7.2 Update template CI/tests/README so compatibility validation runs with existing scan/parity/canary checks and contribution docs describe canonical upstream PRs rather than local-clone staging or generated manual commands.
- [x] 7.3 Update root `README.md`, `packages/daemon/README.md`, CLI help, and other backend-owned live docs for canonical target configuration, typed readiness, managed-root privacy, github.com/`gh auth` prerequisite, preview/submit/receipt semantics, and sanitized-file-only fallback; remove all live `--data-repo` and manual compare-command guidance without editing UI or archived planning material.
- [x] 7.4 Make the development template independently installable by vendoring the coordinated internal `@mosga/*@0.1.0` tarball snapshots, committing its own integrity-pinned lockfile, tracking `vendor/**` in CI, rejecting real user/workspace paths inside archives, and proving exact current-build packing plus `npm ci` and all template checks in a clean copy outside the monorepo.

## 8. Verification and scope gates

- [x] 8.1 Run focused daemon publication/security/CLI tests, Publisher bundle contract tests, template manifest/scan/parity/canary checks, and local managed-workspace Git integration tests; confirm the test harness contains no real GitHub write target or credential.
- [x] 8.2 Run `npm run typecheck`, `npm run build`, and the complete Vitest suite with the repository timeout; fix every failure caused by removing the publisher delivery surface or old daemon APIs.
- [x] 8.3 Run product-scope audits proving live backend code/docs contain no `--data-repo`, `dataRepoPath`, `dataRepoConfigured`, old publish plan/stage/batch routes, emitted commands/manual fallback, response workspace path, or raw external error field, while excluding archives/planning and leaving `packages/ui/**` untouched.
- [x] 8.4 Run `git diff --check` and strict Rasen validation for `github-publication-target-redesign-publication-backend`; confirm the implementation diff is limited to daemon/backend-owned adapters, tests, template/docs, and this child’s artifacts, with no real GitHub fork/push/PR.
