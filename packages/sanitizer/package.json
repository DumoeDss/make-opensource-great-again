{
  "name": "@mosga/sanitizer",
  "version": "0.1.0",
  "description": "Multi-layer, structure-aware secret/PII detection + human-dispositioned redaction core: gitleaks ruleset ingestion (TOML parse + Go RE2 to JS translation), custom rules, three-layer scan, session-scoped deterministic pseudonym mapping, findings/report model, and a disposition/apply engine.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "vendor"
  ],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mosga/contracts": "*",
    "@mosga/session-readers": "*",
    "smol-toml": "^1.3.1",
    "zod": "^4.3.6"
  }
}
